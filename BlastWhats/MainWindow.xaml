<Window x:Class="BlastWhats.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlastWhats"
        mc:Ignorable="d"
        Title="WA Blaster Kampoeng IT" Height="650" Width="950" 
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5" WindowStyle="ThreeDBorderWindow" WindowState="Maximized">

    <Window.Resources>
        <Style x:Key="SideMenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#EAEAEA"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#34495E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2C3E50"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#2C3E50" CornerRadius="10,0,0,10" Margin="5">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="10,20,10,30">
                    <TextBlock Text="🚀 WA Blaster" Foreground="White" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="v1.0" Foreground="#95A5A6" FontSize="12" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom" Margin="0,0,0,5">
                    <Button x:Name="keluarBlast" Click="ExitMenu_Click" Style="{StaticResource SideMenuButtonStyle}"
                            Background="#E74C3C" HorizontalAlignment="Stretch">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="🚪" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Keluar" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <StackPanel DockPanel.Dock="Top">
                    <Button x:Name="BtnScan" Click="BtnScan_Click" Style="{StaticResource SideMenuButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📷" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Scan QR" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnBlast" Click="BtnBlast_Click" Style="{StaticResource SideMenuButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✉️" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Kirim Pesan" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

            </DockPanel>
        </Border>

        <Border Grid.Column="1" Background="White" Margin="0,5,5,5" CornerRadius="0,10,10,0">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
        </Border>
    </Grid>
</Window>